import React from 'react';
import './Card.css';

const Card3 = ({children}) => {
    return (
        <div className="card">
            {children}
        </div>
        
    );
}

export default Card3;
